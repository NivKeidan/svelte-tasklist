export const SECTIONS = {
    DAILY: "Daily",
    UPCOMING: "Weekly",
    FUTURE: "Future"
};

export const CONTEXT_CONFIG = "section-config-context";

export const defaultConfig = {
    showDate: true,
    showTime: true,
    useDayName: false,
};