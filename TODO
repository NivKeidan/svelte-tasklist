check periodically if update is required since date has changed since last opening the app
convert usage to git instead of running server
add undo for actions
add vocal insertion of new entries
clicking c opens calendar view (to see dates and stuff)